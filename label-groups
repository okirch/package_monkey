#!/usr/bin/python3


from repos import RepoDict, UrlCacheStrategy
from products import ProductCatalog, CacheLocation
from packages import Product, Resolver, ResolverWorker, ResolverPreferences, PackageSelector, PackageCollection
from filter import PackageFilter, Classification
from options import Application
from writers import StandardWriter

application = Application('build-dependency-tree')
dummy = application.catalog

store = application.backingStore
store.enableDependencyTreeLookups()
store.enableSourceLookups()

classificationScheme = Classification.Scheme()
packageFilter = PackageFilter(scheme = classificationScheme)
unclassified = packageFilter.makeBinaryGroup("Unclassified")

packageSelector = PackageSelector()
collection = PackageCollection()

resolver = Resolver(backingStore = store)

for pinfo in store.enumerateLatestPackages():
	pkg = store.retrievePackage(pinfo)
	assert(pkg.product == pinfo.product)

	verdict = packageFilter.apply(pkg, pinfo.product)
	if verdict is not None:
		verdict.labelPackage(pkg)
	else:
		# we currently have no good way to properly mark unclassified packages
		# group = unclassified
		pass

	collection.add(pkg)

class LabelTreeTraversal:
	def __init__(self):
		self.alreadySeen = set()
		self.visited = []

	def flatten(self, label):
		return self.flattenWork(label, lambda l: l.runtimeRequires)

	def flattenBuildRequirements(self, label):
		return self.flattenWork(label, lambda l: l.buildRequires)

	def flattenWork(self, label, childAccessor):
		result = []

		if label in self.alreadySeen:
			return result

		self.alreadySeen.add(label)
		for child in childAccessor(label):
			if child not in self.alreadySeen:
				result += self.flattenWork(child, childAccessor)

		self.visited.append(label)
		result.append(label)

		for child in label.flavors:
			result += self.flattenWork(child, childAccessor)

		return result

	def __iter__(self):
		return iter(self.visited)

def flattenGroupList(type, names):
	traversal = LabelTreeTraversal()
	for groupName in names:
		group = packageFilter.makeGroup(groupName, type)
		traversal.flatten(group.label)
	return traversal

binaryGroupNames = ("@Core", "@HostCore", "@Kernel", "@UpdateStack", "@CoreLibraries+odbc", "@X11Libraries", "@MinimalSystemd", "@DesktopBuild")
binaryGroupNames = ("@CoreLibraries", )
binaryTraversal = flattenGroupList(Classification.TYPE_BINARY, binaryGroupNames)
#sourceTraversal = flattenGroupList(Classification.TYPE_SOURCE, ("Core", "KernelPlus"))
sourceTraversal = []

# do we ever define these preferences anywhere?
preferences = ResolverPreferences()

worker = ResolverWorker(resolver, preferences = preferences)
problemLog = worker.problems

for labelName in ("@Gnome", "@Texlive", "@Emacs", "@Vim", "@Qt"):
	label = classificationScheme.getLabel(labelName)
	assert(label)
	print(f"Ignoring all problems for label {label}")
	problemLog.ignore(label)

productArchitecture = store.getProperty("arch")
if productArchitecture is None:
	raise Exception("database does not record architecture of original query")

resolverContext = worker.contextForArch(productArchitecture)
classificationContext = Classification.ClassificationContext(worker, productArchitecture, labelOrder)

def classifyRuntimeDependencies(label):
	global binaryTraversal

	group = packageFilter.makeBinaryGroup(label.name)
	labelledPackages = group.packages

	print(f"Classify packages belonging to {group.label}")

	# Step 0: automatically add compatible build flavors of subordinate groups
	# iff we satisfy all their dependencies
	if True:
		label.autoSelectCompatibleFlavors()
	else:
		update = set()
		for req in label.runtimeRequires:
			for flavor in req.flavors:
				if flavor.allFlavorRequirementsSatisfied(label):
					print(f"{label} auto-selected {flavor} via {req}")
					update.add(flavor)
		for flavor in update:
			# print(f" {label} -> enable {flavor}")
			label.addRuntimeDependency(flavor)

	# Step 1: build the runtime dependency closure
	classifier = Classification.DownwardClosure(classificationContext, label)
	classifier.classify(labelledPackages)
	labelledPackages = classifier.result

	packageFilter.updateGroup(label, labelledPackages)

	# FIXME: we should not do this for groups who are flavors themselves; else
	# we end up with labels like @HostNetworking+gnome+devel+unused
	classifier = Classification.AutoflavorPackageClosure(problemLog, store)
	classifier.classify(labelledPackages)
	for flavorDef in packageFilter.autoFlavors:
		# FIXME this should go around the loop
		if label.flavorBase is not None:
			continue


		found = classifier.getFlavor(flavorDef.name)
		if found is None:
			continue

		if flavorDef.label.disposition == Classification.DISPOSITION_MERGE:
			relabeled = classifier.labelFlavoredPackages(flavorDef.name, label)
			packageFilter.updateGroup(label, relabeled)

			if False:
				print(f"Merged {len(relabeled)} packages into {label}")
				for l in relabeled:
					print(f"  {l}")
		elif flavorDef.label.disposition == Classification.DISPOSITION_IGNORE:
			pass
		else:
			flavor = packageFilter.instantiateAutoFlavor(group, flavorDef)
			relabeled = classifier.labelFlavoredPackages(flavorDef.name, flavor.label)
			packageFilter.updateGroup(label, relabeled)

			# If we instantiate Foo+devel, automatically add make Foo buildrequire Foo+devel
			if flavorDef.name == "devel":
				group.addBuildRequires(flavor)
				# maybe this can happen later
				if group.label.buildConfig:
					group.label.buildConfig.addBuildDependency(flavor.label)

	# Step N: sort all other sibling packages into $Group+unused.
	# To avoid infinite recursion, do not perform this step for the +unused flavor itself.
	if not label.name.endswith("unused"):
		while group.label.flavorBase:
			group = packageFilter.makeBinaryGroup(group.label.flavorBase.name)

		siblingGroup = packageFilter.makeFlavorGroup(group, "unused")
		siblingGroup.label.autoSelect = False

		classifier = Classification.SiblingPackageClosure(problemLog, siblingGroup.label, store)
		classifier.classify(labelledPackages)
		siblingGroup.update(classifier.result)

def classifyBuildDependencies(label):
	global binaryTraversal

	# get the build requirements of this label and make sure we've satisfied all their runtime
	# requirements
	for buildLabel in binaryTraversal.flattenBuildRequirements(label):
		classifyRuntimeDependencies(buildLabel)

	group = packageFilter.makeBinaryGroup(label.name)

	classifier = Classification.BuildRequiresClosure(problemLog, label, store)
	packagesNeededForBuild = classifier.classify(group.packages)

	unlabelledPackages = classifier.unlabelledPackages
	if unlabelledPackages:
		print(f"{label.name} has {len(unlabelledPackages)} unclassified build requirements")

		print(f"Classifying new build requirements for {label}")
		classifier = Classification.UnknownPackageClassifier(worker, productArchitecture, label)
		classifier.classify(unlabelledPackages)

		# FIXME: This should also be part of the problem report
		for suggestedLabel, candidatePackages in classifier.suggestions:
			print(f"   Classifier suggests label {suggestedLabel} for these package(s)")
			for cand in candidatePackages:
				print(f"      {cand.shortname}")

processedBinaryLabels = []

queue = list(binaryTraversal)
while queue:
	label = queue.pop(0)
	classifyRuntimeDependencies(label)

	# After classification, new flavors may have been added automatically
	# Traverse any newly created flavors insert them at the head of the queue
	for flavor in label.flavors:
		queue[:0] = binaryTraversal.flatten(flavor)

for label in binaryTraversal:
	classifyBuildDependencies(label)

for label in sourceTraversal:
	group = packageFilter.makeSourceGroup(label.name)

	print(f"Classify packages belonging to source project {group.label}")

	classifier = Classification.BuildRequireClosure(worker, productArchitecture, label)
	assert(classifier)
	for component in label.buildRequires:
		componentGroup = packageFilter.makeBinaryGroup(component.name)
		assert(componentGroup)
		classifier.classify(componentGroup.packages)

	closure[label.name] = classifier.result

writer = StandardWriter()
writer.write(packageFilter, binaryTraversal)

# writer.write(packageFilter, sourceTraversal)

if problemLog:
	writer.writeProblems(problemLog)
